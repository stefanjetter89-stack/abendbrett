/**
 * Zugangsdaten für die gemeinsame Ablage (Supabase).
 *
 * Beide Werte findest du in deinem Supabase-Projekt unter
 * Settings → API — sie sind zum Veröffentlichen gedacht (der
 * "anon key" ist bewusst öffentlich, die Absicherung passiert über
 * die Row-Level-Security-Regel aus dem README).
 *
 * Trage hier deine echten Werte ein und committe die Datei.
 */
export const SUPABASE_URL = "https://DEIN-PROJEKT.supabase.co";
export const SUPABASE_ANON_KEY = "DEIN-ANON-KEY";
